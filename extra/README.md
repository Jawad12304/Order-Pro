# Extra & Archived Files

This directory contains legacy, supplementary, or alternative configuration files moved out of the root directory to keep the project clean and minimal:

- **Legacy Database**: `orderpro_production_dump.sql` (PostgreSQL dump), `import-to-cloud-db.js`, `db.js` (PostgreSQL scripts)
- **Alternative Deployment Options**: `docker-compose.yml`, `docker-compose.prod.yml`, `.dockerignore`, `railway.json`, `HOSTINGER_DEPLOYMENT_GUIDE.md` (Option A Docker/VPS guide)
- **Documentation & Notes**: `IMPLEMENTATION_ROADMAP.md`, `implementation_plan.md`, `project_overview_and_swot_analysis.md`, `ui ux plan`, `skills-lock.json`, and `scratch/`
